@model IEnumerable<Axe.Models.Technology>
@{
    ViewData["Title"] = "Home Page";
}

<div id="myCarousel" class="carousel slide" data-ride="carousel" data-interval="6000">
    <ol class="carousel-indicators">
    @{
        int idx = 0;
        @foreach (var item in Model)
        {
            var active = idx == 0 ? "active" : null;
            <li data-target="#myCarousel" data-slide-to="@idx" class="@active"></li>            
            idx = idx + 1;
        }
    }
    </ol>

    <div class="carousel-inner" role="listbox">
        @{ 
            int itemIdx = 0;
            @foreach (var tech in Model)
            {
                var itemActive = itemIdx == 0 ? "active" : null;
                itemIdx = itemIdx + 1;

                <div class="item @itemActive">
                    <img src="~/images/sky.svg" alt="ASP.NET" class="img-responsive" />
                    <div class="carousel-caption" role="option">
                        <p class="techCard">
                            <span class="techCardTitle">@tech.Name</span>
                            <br />
                            @Html.DisplayFor(t => tech.InformationText)
                            <br />
                            <br />
                            @Html.ActionLink("Take a test", "Edit", "Technologies", new { id = tech.Id }, new { @class = "btn btn-default" })
                        </p>
                    </div>
                </div>
            }
        }
    </div>

    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>

<!--  todo make metro panel aka https://docs.microsoft.com/en-us/-->
@*<div class="row">
    <ul>
        @foreach (var tech in Model)
        {
            <a href="@Url.Action("Edit", "Technologies", new { id = tech.Id})">
                <li class="techCard">
                    <span class="techCardTitle">@tech.Name</span>
                    <br />
                    @Html.DisplayFor(t => tech.InformationText)
                </li>
            </a>
        }
</ul>
</div>*@

